{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Doodlejump",
            "type": "shell",
            "command": "cd build && cmake --build .",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "Compile the project using CMake."
        }
    ]
}
